# Install all dependencies
pip install torch torchvision opencv-python numpy pandas matplotlib seaborn
pip install ultralytics yolov5  # or use torch.hub

# For better performance (optional)
pip install pycocotools  # for evaluation metrics